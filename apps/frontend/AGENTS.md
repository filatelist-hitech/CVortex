# Frontend scope

M0 is a branded technical shell, not a product dashboard. Consume semantic values derived from `brand/tokens/cvortex.tokens.json`, keep browser API calls same-origin under `/api/v1`, and never expose backend service names or secrets to client code.

Run ESLint, TypeScript, Vitest and the production build after frontend changes.
